{
  "name": "container-pages",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:web": "npm run dev --prefix web",
    "dev:cms": "npm run develop --prefix cms",
    "build": "npm run build --prefix web && npm run build --prefix cms",
    "build:web": "npm run build --prefix web",
    "build:cms": "npm run build --prefix cms",
    "start:web": "npm run start --prefix web",
    "start:cms": "npm run start --prefix cms"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
